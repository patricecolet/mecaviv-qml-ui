# Provenance des binaires de ce dossier — a mettre a jour a chaque copie depuis NiDMI.
firmware_version  = v0.2.0-rc1-13-g5184edc-dirty   (chaine embarquee : « dirty » vient de
                    pd/basic-exemple.pd, modifie dans l'arbre de travail et hors
                    firmware ; les sources compilees sont celles du commit ci-dessous)
firmware_variant  = usbmidi-on+usbnet
source_repo       = https://github.com/patricecolet/NiDMI  (branche feat/usb-net, commit 5184edc,
                    non pousse ; bibliotheque nidmi-core branche feat/usb-ncm-spike, commit 73cda96)
build_command     = ./scripts/nidmi.sh build --board s3 --variant on --usb-net --ota
partition_table   = tools/nidmi_s3_ota_dual_littlefs.csv
built_at          = 2026-09-24
bootloader/partitions : inchanges depuis le build du 2026-08-25 (identiques octet a octet)

nvs_image         = gyrophone-pad.nvs.bin (genere par scripts/build-nvs.sh depuis
                    nvs/gyrophone-pad.csv ; capture depuis la carte de reference,
                    modifiee depuis — voir l'en-tete du CSV). USB seul : wifi_enabled=0,
                    rtp_enabled=0, osc_out_all=0 (cles lues a partir de 5184edc).

valide_sur        = Pi (cdc_ncm) : 20 redemarrages + 2 rebranchements, 22/22 (avec une
                    ligne de debug en plus, retiree ensuite). macOS : lien encore
                    intermittent, ~1 demarrage sur 2 (sujet ouvert cote NiDMI).
